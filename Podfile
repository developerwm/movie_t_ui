# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'movie_t_ui' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 5.2'
  pod 'URLImage'
  
  # Pods for movie_t_ui

  target 'movie_t_uiTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'movie_t_uiUITests' do
    # Pods for testing
  end

end
